let dolares = Number(prompt("Ingrese la cantidad en dolares a convertir:"));

const tipoCambio = 25; 

let lempiras = dolares * tipoCambio;

console.log(`${dolares} dolares son equivalentes a ${lempiras} lempiras`);